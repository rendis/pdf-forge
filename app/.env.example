# URL prefix for all routes (SPA routing, API calls, asset paths)
# Empty = root (/), set to deploy under a sub-path (e.g. /pdf-forge)
# Must match backend's server.base_path config
# VITE_BASE_PATH=/pdf-forge

# Mock auth mode for development (bypasses OIDC)
# VITE_USE_MOCK_AUTH=true

# Note: OIDC config (token endpoint, userinfo, logout, client_id) is fetched
# at runtime from backend {base_path}/api/v1/config. Configure OIDC in backend's app.yaml.
