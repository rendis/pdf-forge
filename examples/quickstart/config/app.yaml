server:
  port: "8081"

database:
  host: localhost
  port: 5433
  user: postgres
  password: postgres
  name: pdf_forge
  ssl_mode: disable

# auth: omitted → dummy mode (no auth needed)

typst:
  bin_path: typst
  timeout_seconds: 10
