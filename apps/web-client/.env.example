# Backend API (default: relative URL works when frontend served by backend)
# VITE_API_URL=/api/v1
# For dev with separate frontend server (pnpm dev), use absolute URL:
# VITE_API_URL=http://localhost:8080/api/v1

# OIDC Authentication (required for production with real auth provider)
# Leave commented for dummy auth mode (backend auto-logins as admin)
# VITE_OIDC_TOKEN_URL=http://localhost:8180/realms/your-realm/protocol/openid-connect/token
# VITE_OIDC_USERINFO_URL=http://localhost:8180/realms/your-realm/protocol/openid-connect/userinfo
# VITE_OIDC_LOGOUT_URL=http://localhost:8180/realms/your-realm/protocol/openid-connect/logout
# VITE_OIDC_CLIENT_ID=web-client
