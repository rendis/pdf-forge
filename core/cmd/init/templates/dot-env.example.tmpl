# {{.ProjectName}} development defaults
# Copy to .env: cp .env.example .env

# Database (matches docker-compose.yaml)
DOC_ENGINE_DATABASE_PASSWORD=postgres

# Auth: dummy mode (no OIDC required)
# To enable OIDC, configure auth section in settings/app.yaml
